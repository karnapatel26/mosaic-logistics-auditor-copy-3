# Mosaic Logistics Billing Auditor Frontend

Next.js App Router implementation for the Mosaic Logistics Billing Auditor. The active dashboard lives in `app/components/DashboardClient.tsx`; data ingestion and reconciliation live in `lib/fetchData.ts` and `lib/reconciliation.ts`.

See the repository root `README.md` for setup, features, assumptions, and methodology.
